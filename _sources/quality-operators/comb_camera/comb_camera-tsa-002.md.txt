(comb_camera-tsa-002)=
<div class="quality-page-kicker">QUALITY OPERATOR · time_sample_alignment</div>

# `comb_camera-tsa-002` — 八路画面是否全部同步配对成功

[所属模态算子：Comb Camera](../../modality-operators/comb_camera.md)

<div class="quality-meta">
  <code class="meta-lifecycle is-active">active</code>
  <code class="meta-artifact is-available">error 时产物可生成</code>
  <code class="meta-cardinality">single</code>
  <code class="meta-domain">time_sample_alignment</code>
  <code class="meta-stage">raw</code>
  <code class="meta-version">rule v2.0.0</code>
  <code class="meta-version">fm-comb-camera-operator v0.4.0</code>
</div>

<p class="quality-lead">以每条 cam0 画面为基准，从其余 7 路选择未使用的最近画面，计算时间跨度合格的完整八目组比例。</p>

## 先看懂这条规则

- **检查什么：** 对每张 cam0 画面，在 cam1 到 cam7 各找一张时间最接近且没有被前面分组用过的画面，组成一组后再检查最早与最晚画面的时间差。
- **什么情况下不通过：** 只要有一张 cam0 画面缺少某路近邻、只能复用旧画面，或整组时间差超过允许范围，配对率就低于必须达到的 100%。

### 判定标准

| 状态 | 怎么判 |
| --- | --- |
| **`pass`** | `accepted_groups == denominator_messages`，因此 value 必须精确等于 1.0 |
| **`warn`** | 不会出现；本规则不使用 warn 状态。 |
| **`error`** | 任一 cam0 消息未能成组，即 value < 1.0 |

**边界怎么算：**

- 30 FPS 的候选组跨度恰好 10 ms 时允许通过；严格大于 10 ms 才拒绝。


## 版本历史

| Rule 版本 | 首次对应包版本 | 更新时间 | 变更 | 提交 |
| --- | --- | --- | --- | --- |
| — | — | — | 尚无基线后的版本变更 | — |

仅展示文档 1.0.0 基线之后的变更。

## 计算契约

::::{admonition} 计算契约（不可直接调用）
:class: quality-contract

```python
quality.comb_camera.comb_camera_tsa_002(
    camera_timelines: int64 timestamps and source indices cameras=[8], messages=[T_c],
    *,
    expected_rate_hz: float,
) -> RuleResult
```
::::

::::{div} quality-source-links
[implementation](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/src/fm_comb_camera_operator/quality_checks.py#L236) ·
[registry](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/src/fm_comb_camera_operator/rules.py#L53) ·
[test 1](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/tests/test_comb_camera_operator.py#L454) · [test 2](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/tests/test_comb_camera_operator.py#L542) ·
[计算规范](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/docs/quality-rule-calculation-spec.md#74-%E5%85%AB%E7%9B%AE%E6%9C%80%E8%BF%91%E6%97%B6%E9%97%B4%E9%85%8D%E5%AF%B9)
::::

```{admonition} 注意
:class: note

- 候选组跨度超限时不会消费任何相机帧；已经接受的组中，8 个源帧索引都永不复用。等距候选选择时间较新的帧。
- `strict_sync` 仅表示组跨度不超过 5 ms，便于下游选择更严格子集；正式 pass 上限在 30 FPS 下是闭区间 10 ms。
- error 时只要 `accepted_groups > 0`，仍会生成结构安全的部分配对索引；字段包括 `timestamps[M]`、`frame_indices[M,8]`、`sync_timestamps_ns[M,8]`、`group_span_ms[M]` 和 `strict_sync[M]`。
```

## 数据输入

| 名称 | 来源 | dtype | shape | 单位 | 必需 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| `camera_timelines` | raw | `int64 timestamps and source indices` | `cameras=[8], messages=[T_c]` | ns | 是 | 已通过完整解码和逐路时间检查的八路消息；包含每条消息的 `sync_ts_ns` 与该相机内的零基源索引。 |

## 配置参数

| 名称 | 类型 | 默认值/必需 | 来源 | 说明 |
| --- | --- | --- | --- | --- |
| `expected_rate_hz` | `float` | 必填 | pipeline | 决定允许的组跨度：恰好 30 FPS 时固定为 10 ms，其他帧率暂按 `round(1e9 / expected_rate_hz)` 纳秒。 |

## 计算定义
计算使用 `camera_timelines`，
结果类型为 `number`，
单位为 ratio。

### 计算方法

```text
for each cam0 message in original order:
  candidate[c] = nearest not-yet-used frame in camera c, for c = 1..7
  # equal absolute distance selects the newer timestamp
  group_span_ns = max(t_cam0, ..., t_cam7) - min(t_cam0, ..., t_cam7)
  accept only if every candidate exists and group_span_ns <= span_limit_ns
  consume all 8 frame indices only after the group is accepted
value = accepted_groups / total_cam0_messages
span_limit_ns = 10_000_000 at 30 FPS
                else round(1e9 / expected_rate_hz)
```

- **聚合口径：** 每个 MCAP 发布一个文件级标量；cam0 的原始消息数是固定分母，外层不按相机拆分实例。
- **无法计算时：** 输入、解码或逐路时间任一前置规则失败时不执行；按输入规则约束，正常进入本规则时 cam0 至少有一条消息。


## 执行关系

**前置质检算子：**

- [`comb_camera-idc-001`](../comb_camera/comb_camera-idc-001.md)
- [`comb_camera-dsf-001`](../comb_camera/comb_camera-dsf-001.md)
- [`comb_camera-tsa-001`](../comb_camera/comb_camera-tsa-001.md)

**不执行条件：**

- 任一输入、解码或逐路时间前置规则未通过时不执行。

**产物影响：** 本规则为 `error` 时，只记录质量异常，不阻止生成结构上有效的产物。
如果后续处理或写入本身失败，仍可能没有产物；最终事实以 `processed_ok` 与 `artifacts`
为准。

**相关质检算子：**

- [`comb_camera-ppa-002`](../comb_camera/comb_camera-ppa-002.md)

## 输出

| 字段 | 约定 |
| --- | --- |
| 实例模型 | `single` |
| `value` | `number` |
| `unit` | ratio |
| `threshold` | `mapping` |
| `details` | `message`, `category`, `accepted_groups`, `rejected_groups`, `span_limit_ms`, `denominator_messages`, `failure_counts`, `rejected_cam0_indices`, `strict_sync_groups`, `strict_sync_limit_ms` |

```json
{
  "rule_id": "comb_camera-tsa-002",
  "rule_version": "2.0.0",
  "problem_domain": "time_sample_alignment",
  "stage": "raw",
  "status": "error",
  "value": 0.5,
  "unit": "ratio",
  "threshold": {
    "expected": 1.0
  },
  "details": {
    "message": "并非所有 cam0 原始消息都能组成未复用且跨度合格的八目组",
    "category": "alignment",
    "denominator_camera": "cam0",
    "denominator_messages": 2,
    "accepted_groups": 1,
    "rejected_groups": 1,
    "span_limit_ms": 10.0,
    "failure_counts": {
      "no_unused_frame": 1
    },
    "rejected_cam0_indices": [
      1
    ],
    "strict_sync_groups": 1,
    "strict_sync_limit_ms": 5.0
  }
}
```

## 失败后建议

1. 先看 `failure_counts`：`no_unused_frame` 表示至少一路没有剩余近邻，`span_exceeded` 表示八路都有候选但组内时间跨度过大。
2. 用 `rejected_cam0_indices` 找到失败基准帧，再对比八路 `sync_ts_ns`，检查固定时钟偏移、帧率不一致、某一路漏帧或乱序。
3. 若只有 `strict_sync_groups` 偏少但规则仍 pass，说明所有组满足正式上限、但部分超过更严格的 5 ms 诊断线；不要把 strict_sync 当成本规则失败条件。

## 来源与维护

- 页面 owner：`data-quality/comb-camera`
- 所属模态算子：[Comb Camera](../../modality-operators/comb_camera.md)
- 实现：[operators/fm-comb-camera-operator/src/fm_comb_camera_operator/quality_checks.py](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/src/fm_comb_camera_operator/quality_checks.py#L236)
- 注册：[operators/fm-comb-camera-operator/src/fm_comb_camera_operator/rules.py](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/src/fm_comb_camera_operator/rules.py#L53)
- 测试：[operators/fm-comb-camera-operator/tests/test_comb_camera_operator.py::test_sync_group_span_threshold_is_ten_inclusive_milliseconds_at_30_fps](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/tests/test_comb_camera_operator.py#L454)
- 测试：[operators/fm-comb-camera-operator/tests/test_comb_camera_operator.py::test_sync_requires_every_cam0_message_without_ninety_percent_override](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/operators/fm-comb-camera-operator/tests/test_comb_camera_operator.py#L542)
- 计算规范：[docs/quality-rule-calculation-spec.md#74-八目最近时间配对](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator/docs/quality-rule-calculation-spec.md#74-%E5%85%AB%E7%9B%AE%E6%9C%80%E8%BF%91%E6%97%B6%E9%97%B4%E9%85%8D%E5%AF%B9)
- [编辑本页 manifest](https://github.com/OriginFlow-AI/AI-Infra-Toolbox/blob/38bfa81fc97b472b1a30066c2a7106c0514fadb3/QC-operator-docs/site/operator-pages/comb_camera/comb_camera-tsa-002.yaml#L1)
